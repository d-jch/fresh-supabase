# Upstream fixtures

This directory will contain committed output from exact, recorded versions of
the official Fresh initializer. The initial target is Fresh 2.3.3 generated with
Deno 2.9.3, in Tailwind and no-Tailwind variants.

Do not copy an existing application here and do not regenerate from `latest`
during ordinary tests.
