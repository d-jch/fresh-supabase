# with-supabase example placeholder

Phase 4 will generate this example by applying the repository CLI to a pinned
official Fresh fixture. The result must also pass independent build, structure,
and authentication smoke tests; it will not serve as the CLI's only correctness
oracle.

No existing project may be copied into this directory.
