# Examples

`with-supabase` is generated from the pinned official Fresh 2.3.3 Tailwind
fixture by installing `password-based-auth` with this repository's CLI.
`provenance.json` records its source, requested block, and committed file
hashes. The example carries its own standalone lockfile.

Normal CI verifies the committed example; it does not regenerate it.
