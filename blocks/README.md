# Embedded blocks

Phase 0 defines the catalog but does not include installable block metadata or
templates. Phase 1 will add declarative definitions for `daisyui`,
`supabase-client`, and `password-based-auth` under the constraints in
`docs/block-format-draft.md`.
